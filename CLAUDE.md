## Spec-Driven Development

This project was built using Spec-Driven Development (SDD). The specification, design, tasks, and
validation all live in `.specs/features/SEZ-1-calculator-mvp/`.

Future sessions should:

1. Read `.specs/STATE.md` (decisions, handoff notes)
2. Keep `docs/PROMPTS.md` updated with every new user prompt — append each prompt verbatim as soon as it's received, before starting other work, using the delimiter format already established in that file (`----...` rule, then a fenced ```text block). Do this for every prompt, including follow-ups and corrections, unless the user explicitly says not to log that one.
3. Re-read this note each session
