# Base URL of the backend API (build-time constant, per Vite env var semantics).
# Falls back to http://localhost:8090 in src/api/calculate.ts when unset.
# In docker-compose.yml this is baked in at image build time via the `args` build arg.
VITE_API_BASE_URL=http://localhost:8090
